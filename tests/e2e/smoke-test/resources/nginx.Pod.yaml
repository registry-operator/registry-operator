apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1
    ports:
    - containerPort: 80
    resources:
      limits:
        memory: "64Mi"
        cpu: "50m"
